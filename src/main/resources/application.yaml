spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/restaurant_platform
    username: postgres
    password: wa..Z,X860@aaa2s[20S3__@_21872811
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

    open-in-view: false

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
    io.restaurant.platform.multitenant: debug

app:
  jwt:
    secret: ${JWT_SECRET:tCgC+u9/QMr36Yo4Zc3nJpsMi56jB3PHn5vLEqdxUtw=}
    expiration: ${JWT_EXPIRATION:86400000} # 24 horas en milisegundos
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174}

server:
  address: 0.0.0.0
  port: ${PORT:8080}